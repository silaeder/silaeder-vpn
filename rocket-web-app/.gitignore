# ignore build directory
/target/

# ignore databases but leave directory
/db/*
!/db/DB_DIRECTORY

# ignore cache but leave
/cache_files/*
!/cache_files/CACHE_DIRECTORY

# idk what is it
# These are backup files generated by rustfmt
**/*.rs.bk
